> ## Tauada multiplicar com enquanto

```
Algoritmo "TABUADA MULTIPLICAR WHILE"
// Lógica de Programação 
// Professor   : Marcos Monteiro  
// Descrição   : Tabuada de multiplicar solicitada pelo usuário
// Autor(a)    : Daniel Pinheiro

   tabuada, contador: inteiro

Inicio
      escreval("Qual a tabuada de multiplicar que você quer: ")
      leia(tabuada)
      contador <- 1
      enquanto contador < 11 faca
           escreval(tabuada," X ",contador," = ", tabuada * contador)
           contador <- contador + 1
      fimenquanto


Fimalgoritmo
```